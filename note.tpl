<html>
<style type="text/css">

	.notilitus { font-size: 1.05em; color: #181819; line-height: 150%; }

/* styles */
/* ====== */
    
    /* headings */
    h1, h2, h3, h4, h5, h6 { font-weight: bold; }
    h1 { font-size: 1.875em; /*30 / 16*/ line-height: 1.6em; /* 48 / 30*/ }
    h2, h3 { font-size: 1.3125em; /*21 / 16*/ line-height: 1.15; /*24 / 21*/ margin-top: 2.285714em; /*48 / 21*/ margin-bottom: 1.15em; /*24 / 21*/ }
    h3 { font-weight: normal; }
    h4 { font-size: 1.125em;/*18 / 16*/ margin-top: 2.67em; /*48 / 18*/    }
    h5, h6 { font-size: 1em; /*16*/ }
    
    /* links */
    a { color: #149718; text-decoration: none;}

    /* block spacing */
    p, blockquote, div { margin-bottom: 1.5em; }
    h1, h2, h3, h4, h5, h6 { margin-bottom: 1.5em; }
    div.readableLargeImageContainer, div.readableLargeObjectContainer { margin-bottom: 1em; margin-top:1em; }
    
    /* blockquote */
    blockquote { font-style: italic; border-left: 5px solid #d4d4d4; margin-left: 2em; padding-left: 1em; }

    /* lists */
    ul, ol { margin: 0 0 1.5em 1.5em; }
    ol li { list-style-type: decimal; list-style-position: outside; }
    ul li { list-style-type: disc; list-style-position: outside; }

    /* tables */
    table { margin-bottom: 1.5em; /*24 / 16*/ font-size: 1em; /* width: 100%; */ }
    thead th, tfoot th { padding: .25em .25em .25em .4em; text-transform: uppercase; }
    th { text-align: left; }
    td { vertical-align: top; padding: .25em .25em .25em .4em; }
    
    /* formatting */
    em, i { font-style: italic; }
    strong, b { font-weight: bold; }

    /* preformatted */
    pre, code, tt { font-size: .875em; line-height: 1.714285em; }

    /* some fixes */
    h1 { line-height: 1.3em; font-weight: normal;  margin-bottom: 0.5em; }
    h1 + h2 { margin-top: .2em; margin-bottom: 2.15em; font-size: .875em; }
    p + ul, h2 + ul, p + ol, h2 + ol { margin-top:-1em; }
    h3 + ul, h4 + ul, h5 + ul, h6 + ul, h3 + ol, h4 + ol, h5 + ol, h6 + ol { margin-top: .5em; }
    h2, h3 { margin-bottom: .75em; }
    hr { border-top: none; border-right: none; border-bottom: 1px solid; border-left: none; }
    pre code, code pre { font-size: 1.15em; }
</style>
<div class = "notilitus">{content}</div>
</html> 
